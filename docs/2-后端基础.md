# learn-backend 电商第一轮：数据库、TodoList 与网页登录

> 先给每天的“待会儿再做”找个地方：记下来、勾掉它，下次打开还能接着用。

本轮先学习数据库，再完成一个支持网页登录的简单 TodoList，并通过 Docker 运行。用户功能会继续用于后续电商项目，这一轮先把待办和登录做好。

## 推荐技术与学习资料

- 数据库：MySQL；
- Java：JDK 17+（上限 25）、Spring Boot 3.5.x，搭配兼容版本的 JDBC、MyBatis/MyBatis-Plus 或 Spring Data JPA；
- Go：Hertz 或 go-zero，搭配 GORM；
- 运行环境：Docker、Docker Compose；
- 测试：Java 可使用 JUnit，Go 可使用标准测试工具。

语言和框架可以替换，实际组合必须完成本轮功能、测试和 Docker 运行要求。版本原则及 Java 资料见[第 0 轮](0-开始之前.md)，Go 资料见[Go 推荐资料](0-推荐资料-go.md)。

使用 AI 排查问题时，提供环境版本、当前尝试、预期结果和实际错误；对版本和配置结论进行实际验证。

- [提示词一：安排本轮学习](prompts/round2/prompt1.md)
- [提示词二：学习数据库基础](prompts/round2/prompt2.md)
- [提示词三：解决 Web 开发中的具体问题](prompts/round2/prompt3.md)
- [提示词四：运行与验证 Docker 项目](prompts/round2/prompt4.md)
- [提示词五：排查具体故障](prompts/round2/prompt5.md)

## 项目要求

### 1. 数据库

学习 MySQL 的表、字段、数据类型、主键、约束和基本 SQL 操作，并在项目中完成用户与待办数据的增删改查。

- 自行设计数据表，用户和 Todo 数据保存在 MySQL 中；
- 提供建表或迁移脚本，使空数据库能够完成初始化；
- 应用重启后，已保存的用户和待办仍可正常使用。

### 2. TodoList

实现待办的 HTTP 接口，支持：

- 创建待办；
- 查询列表和单条详情；
- 修改内容、标记完成或未完成；
- 删除待办。

待办至少包含标题、内容、完成状态和必要的时间信息。接口路径和数据结构自行设计，记录在接口文档中。非法参数、待办不存在或操作失败时，应返回明确结果。

### 3. 网页登录与鉴权

提供一个可在浏览器中操作的简单页面，完成：

- 注册、登录和退出；
- 查看、修改自己的基本信息；
- 修改密码；
- 登录后使用 TodoList。

需要实现鉴权：未登录时不能访问受保护的数据或执行相关操作；用户只能查看、修改和删除自己的待办与个人信息。登录失效或退出后，应重新登录才能继续访问受保护资源。

页面需要完成操作并展示结果和错误提示；视觉设计不计入本轮考核，可以使用简单页面或现成 UI 模板。

### 4. Docker

自行编写应用的 `Dockerfile` 和 `compose.yaml`，使用 Docker Compose 启动应用与 MySQL。启动后能够从浏览器完成上述操作。

提交的配置应支持数据库初始化，并提供所需配置的示例值。在保留业务数据的正常停止、再次启动后，已有用户和待办仍可使用。README 写明构建、启动、访问、停止和测试方式。

## 测试与异常处理

延续起步任务的要求：实现功能时同步验证正常、边界和失败情况，修复问题后保留回归测试。

自动化测试至少覆盖：

- Todo 的增删改查，以及非法参数、目标不存在的情况；
- 注册、登录、退出、个人信息和修改密码；
- 重复注册、错误密码、未登录访问；
- 用户访问或修改其他用户的数据；
- 登录失效及退出后的受保护接口访问；
- 数据库操作失败时，接口返回失败，不能伪造成功或留下不符合业务规则的数据。

测试要断言预期结果，不只打印响应。另记录一次浏览器操作和 Docker 启停验证，检查核心功能、错误提示及数据保留情况。

密码不得明文存储，接口和日志不应泄露密码、登录凭据或内部错误细节；配置示例不包含真实密钥。异常应有便于排查的记录，不能吞掉错误后继续返回成功。

## 提交内容

从本轮开始，自行创建一个**独立的个人公开项目仓库**，提交链接到负责人指定入口。第三、第四轮继续在该商城仓库迭代，分别保留 Git 标签或 Release，不把后续项目源码交到本指南仓库。本轮提交包含：

- 后端源码、简单网页和依赖配置；
- 数据库建表或迁移脚本；
- `Dockerfile`、`compose.yaml` 和配置示例；
- 接口文档、自动化测试和 README。

README 写清运行环境、初始化与启动步骤、访问地址、测试方式与实际结果，以及未完成内容和已知问题。别人按说明应能启动项目并完成注册、登录和待办操作。

本轮需要答辩，时间以当届通知为准。遇到问题时，带上代码、错误和排查过程及时沟通。

## Bonus

完成基础功能和测试后，可以选做：

- 待办筛选、排序或分页；
- 待办截止时间；
- 可交互的接口文档。

额外功能不抵消缺失的数据库、鉴权、Docker 或测试要求。
